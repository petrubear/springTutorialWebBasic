#Mensajes de error
#el formato debe ser constraitname.bean.field

#User
DuplicateKey.user.username = "This `username` already exists!"
Size.user.username = "Invalid `username`. It must be 4-15"
NotBlank.user.username = "`username` is required"
Pattern.user.username = "`username` must be alphanumeric"
Size.user.password = "Invalid `password`. It must be 4-15"
NotBlank.user.password = "`password` is required"
Pattern.user.password = "`password` must not have spaces"
Size.user.email = "Invalid `email`. It must be 4-60"
NotBlank.user.email = "`email` is required"
CustomEmailValidator.user.email = "`email` is not valid"
Size.user.name = "Invalid `name`. It must be 3-60"
NotBlank.user.name = "`name` is required"

#Offer
#Size.offer.name = "Invalid `name`. It must be 5-20"
#Size.offer.text = "Invalid `text`. It must be 5-100"
CustomEmailValidator.offer.email = "`email` is not valid"

#New Account Passwords
PasswordMatch.password.match = "passwords match!"
PasswordMatch.password.dontmatch = "passwords do not match!"
